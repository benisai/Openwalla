
const express = require('express');
const router = express.Router();

// Placeholder for rules routes
router.get('/', (req, res) => {
  res.json({ message: 'Rules endpoint - not implemented yet' });
});

module.exports = router;
